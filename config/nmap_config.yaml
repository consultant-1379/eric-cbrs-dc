nmapConfig:
  services:
    - name: "eric-cbrs-dc"
      protocol:
        - "tcp"
      additionalParams:
        - "-n"
        - "-p-"
        - "-sV"
        - "--reason"
        - "--webxml"
  reportDir: "nmap_reports"